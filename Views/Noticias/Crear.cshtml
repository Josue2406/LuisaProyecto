@model ProyectoLuisa.Models.Noticia
@{
    ViewData["Title"] = "Crear Noticia";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";

    // 👉 Solución: si Model es null, creamos un objeto vacío
    var noticia = Model ?? new ProyectoLuisa.Models.Noticia();
}

<h2 class="mb-4 fw-bold text-primary">📰 Crear Noticia</h2>

<form asp-action="Crear" method="post" enctype="multipart/form-data" class="shadow p-4 bg-light rounded">
    @Html.AntiForgeryToken()

    @await Html.PartialAsync("Formulario", noticia)

    <div class="mt-4 text-end">
        <button class="btn btn-success px-4 fw-bold">
            <i class="bi bi-save"></i> Guardar
        </button>
        <a asp-action="Index" class="btn btn-secondary px-4">
            <i class="bi bi-arrow-left"></i> Volver
        </a>
    </div>
</form>
