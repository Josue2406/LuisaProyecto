@model IEnumerable<ProyectoLuisa.Models.HorarioCard>
@{
    Layout = "~/Views/Public/Shared/_LayoutPublic.cshtml";
    ViewData["Title"] = "Horarios Escolares";
}

<h2 class="text-center fw-bold text-primary mb-4">🕓 Horarios</h2>

<div class="row row-cols-1 row-cols-md-4 g-4">

@foreach (var h in Model)
{
    <div class="col">
        <div class="card shadow-sm h-100">

            <a asp-action="Detalle" asp-route-id="@h.Id">
                <img src="@h.ImagenUrl" class="card-img-top" style="height:150px; object-fit:cover;" />
            </a>

            <div class="card-body">
                <h5 class="fw-bold">@h.Grado @h.Grupo – @h.Seccion</h5>
                <p class="text-muted">Subido por @h.SubidoPorNombre</p>
                <a asp-action="Detalle" asp-route-id="@h.Id" class="btn btn-primary btn-sm">Abrir</a>
            </div>

        </div>
    </div>
}

</div>
