@model IEnumerable<ProyectoLuisa.Models.HorarioImagen>

@{
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
    ViewData["Title"] = "Gestión de Horarios";
}

<h2 class="mb-4 text-primary">📚 Horarios (Administración)</h2>

<a asp-action="Crear" class="btn btn-success mb-3">
    <i class="bi bi-plus-circle"></i> Subir Horario
</a>

<div class="row row-cols-1 row-cols-md-3 g-4">
@foreach (var h in Model)
{
    <div class="col">
        <div class="card shadow-sm h-100">
            <img src="@h.ImagenUrl" class="card-img-top" style="height:180px; object-fit:cover;" />

            <div class="card-body">
                <h5 class="fw-bold">Grado @h.Grado - Sección @h.Seccion</h5>
                <p class="text-muted small">Docente: @h.Docente</p>
                <p class="text-muted small">Subido por: @h.SubidoPor</p>
            </div>

            <div class="card-footer text-center">
                <a asp-action="Editar" asp-route-id="@h.Id" class="btn btn-warning btn-sm">Editar</a>

                <form asp-action="Eliminar" asp-route-id="@h.Id" method="post" class="d-inline">
                    <button class="btn btn-danger btn-sm">Eliminar</button>
                </form>
            </div>
        </div>
    </div>
}
</div>
