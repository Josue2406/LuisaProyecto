@{
    ViewData["Title"] = "Gestión de Roles";
    var roles = ViewBag.Roles as IEnumerable<dynamic>;
}

<div class="container mt-4">
    <h2 class="text-center mb-4 text-primary">👥 Panel de Administración - Roles</h2>

    @if (TempData["Mensaje"] != null)
    {
        <div class="alert alert-success text-center">@TempData["Mensaje"]</div>
    }
    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger text-center">@TempData["Error"]</div>
    }

    <div class="d-flex justify-content-between mb-3">
        <form method="post" asp-action="Crear" class="d-flex gap-2">
            <input type="text" name="nombreRol" class="form-control" placeholder="Nuevo rol..." required />
            <button type="submit" class="btn btn-success">Crear Rol</button>
        </form>
        <a href="/Admin" class="btn btn-secondary">Volver al panel</a>
    </div>

    <table class="table table-hover align-middle shadow-sm">
        <thead class="table-primary">
            <tr>
                <th>Rol</th>
                <th>Estado</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var r in roles)
            {
                <tr>
                    <td>@r.Nombre</td>
                    <td>
                       @if (r.Nombre != "Admin")
    {
        <div class="btn-group">
            @if (r.Activo)
            {
                <!-- 🔸 Si el rol está activo -->
                <form method="post" asp-action="Desactivar" class="d-inline">
                    <input type="hidden" name="nombreRol" value="@r.Nombre" />
                    <button type="submit" class="btn btn-sm btn-outline-warning">
                        <i class="bi bi-pause-circle"></i> Desactivar
                    </button>
                </form>
            }
            else
            {
                <!-- 🔹 Si el rol está inactivo -->
                <form method="post" asp-action="Reactivar" class="d-inline">
                    <input type="hidden" name="nombreRol" value="@r.Nombre" />
                    <button type="submit" class="btn btn-sm btn-outline-success">
                        <i class="bi bi-play-circle"></i> Reactivar
                    </button>
                </form>
            }
              <!-- 🗑️ Eliminar siempre disponible -->
            <form method="post" asp-action="Eliminar" class="d-inline">
                <input type="hidden" name="nombreRol" value="@r.Nombre" />
                <button type="submit" class="btn btn-sm btn-outline-danger"
                        onclick="return confirm('¿Eliminar el rol @r.Nombre? Esto desactivará sus usuarios y lo quitará del sistema.');">
                    <i class="bi bi-trash"></i> Eliminar
                </button>
            </form>
        </div>
    }
    else
    {
        <span class="text-muted">No modificable</span>
    }
                    </td>
                    <td>
                        @if (r.Nombre != "Admin")
                        {
                            <div class="btn-group">
                                @if (r.Activo)
                                {
                                    <form method="post" asp-action="Desactivar" class="d-inline">
                                        <input type="hidden" name="nombreRol" value="@r.Nombre" />
                                        <button type="submit" class="btn btn-sm btn-outline-warning">
                                            <i class="bi bi-pause-circle"></i> Desactivar
                                        </button>
                                    </form>
                                }
                                <form method="post" asp-action="Eliminar" class="d-inline">
                                    <input type="hidden" name="nombreRol" value="@r.Nombre" />
                                    <button type="submit" class="btn btn-sm btn-outline-danger"
                                            onclick="return confirm('¿Eliminar el rol @r.Nombre? Esto desactivará sus usuarios y lo quitará del sistema.');">
                                        <i class="bi bi-trash"></i> Eliminar
                                    </button>
                                </form>
                            </div>
                        }
                        else
                        {
                            <span class="text-muted">No modificable</span>
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
